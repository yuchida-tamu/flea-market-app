<% if users.any? %>
    <ul class="list-unstyled">
        <% users.each do |user| %>
            <li>
                <%= user.name %>
                <%= link_to 'View profile', user_path(user) %>
            </li>
        <% end %>
    </ul>
    <%= paginate users %>
<% end %>